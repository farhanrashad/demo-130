<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
      
    <record id="purchase_demand_calendar_view" model="ir.ui.view">
            <field name="name">purchase.demand.calendar.view</field>
            <field name="model">purchase.demand</field>
            <field name="arch" type="xml">
                <calendar string="Material Requisition" date_start="date_requisition" color="state" hide_time="true" event_limit="5">
                    <field name="user_id" avatar_field="image_128" invisible="1"/>
                    <field name="state" filters="1" invisible="1"/>
                </calendar>
            </field>
    </record>
      
    <record id="purchase_demand_tree_view" model="ir.ui.view">
            <field name="name">purchase.demand.tree.view</field>
            <field name="model">purchase.demand</field>
            <field name="priority">2</field>
            <field name="arch" type="xml">
                <tree string="Purchase Requisition" multi_edit="1" sample="1">
                    <field name="message_needaction" invisible="1"/>
                    <field name="name" string="Number" readonly="1" decoration-bf="1"/>
                    <field name="date_requisition" string="Requisition Date" widget="date" optional="show"/>
                    <field name="date_deadline" optional="hide"/>
                    <field name="schedule_date" optional="hide"/>
                    <field name="employee_id" optional="show" />
                    <field name="user_id" optional="show" widget="many2one_avatar_user" invisible="1"/>
                    <field name="activity_ids" widget="list_activity" optional="show"/>
                    <field name="company_id" groups="base.group_multi_company" optional="show" readonly="1"/>
                    <field name="state" />
                </tree>
            </field>
    </record>
      
    <record id="purchase_demand_form_view" model="ir.ui.view">
        <field name="name">purchase.demand.form.view</field>
        <field name="model">purchase.demand</field>
        <field name="arch" type="xml">
            <form string="Purchase Requisition" class="" >
                <header>
                    <button name="action_confirm" string="Confirm" type="object" class="oe_highlight"
                        attrs="{'invisible': [('state', 'not in', ['draft'])]}"/>
                    
                    <button name="action_purchase_transfer" type="object" class="oe_highlight"
                    string="New Quotation" attrs="{'invisible': [('state', '!=', 'in_progress')]}"/>

                    <button name="action_done" type="object" class="oe_highlight"
                    string="Validate" attrs="{'invisible': [('state', '!=', 'in_progress')]}"/>
                    
                    
                    <button name="action_cancel" type="object" string="Cancel" attrs="{'invisible': ['|', ('state', 'not in', ['draft', 'in_progress']), ('id', '=', False)]}"/>
                    
                    <button name="action_draft" states="cancel" type="object" string="Set to Draft"/>
                    
                    <field name="state" widget="statusbar" statusbar_visible="draft,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(action_requisition_purchase_order_list)d" type="action" class="oe_stat_button" icon="fa-list-alt" attrs="{'invisible': ['|',('state', '=', 'draft'),('order_count', '=', 0)]}">
                        <field name="order_count" widget="statinfo" string="RFQs/Orders"/>
                        </button>
                       
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group name="requisition_header">
                        <group name="partner_details">
                            <field name="user_id"/>
                            <field name="employee_id" widget="res_partner_many2one" options='{"always_reload": True}'/>
                            <field name="department_id" />
                            <field name="user_id" domain="[('share', '=', False)]" widget="many2one_avatar_user" invisible="1"/>
                        </group>
                        <group name="requisition_details">
                            <field name="demand_type_id" options="{'no_create':True, 'no_open':True}"/>
                            <field name="date_requisition" />
                            <field name="date_deadline" />
                            <field name="schedule_date" />
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" attrs="{'readonly': [('state','not in',('draft'))]}"/>

                        </group>
                    </group>
                    <notebook>
                        <page string="Products" name="requisition_line">
                            <field name="purchase_demand_line" widget="one2many_list">
                                <tree string="Requisition lines" editable="bottom">
                                    <field name="demand_action"/>
                                    <field name="product_id" required="1"/>
                                    <field name="product_template_id" invisible="1"/>
                                    <field name="name"/>
                                    <field name="product_uom_qty"/>
                                    <field name="ordered_qty" />
                                    <field name="product_uom"/>
                                    <field name="schedule_date" />
                                    <field name="partner_id" attrs="{'invisible': [('demand_action','!=','purchase')],'required':[('demand_action','=','purchase')]}"/>
                                    <field name="product_uom_category_id" invisible="1"/>
                                </tree>
                            </field>
                        <separator string="Terms and Conditions"/>
                        <field name="description" attrs="{'readonly': [('state','not in',('draft','in_progress'))]}"/>

                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
      
    <record id="purchase_demand_search_view" model="ir.ui.view">
        <field name="name">purchase.demand.filter.view</field>
        <field name="model">purchase.demand</field>
        <field name="priority" eval="15"/>
        <field name="arch" type="xml">
            <search string="Search Requistion">
                <field name="name" string="Reference" filter_domain="['|', ('name', 'ilike', self), ('origin', 'ilike', self)]"/>
                <field name="user_id" invisible="1"/>
                <field name="employee_id" operator="child_of"/>
                <filter string="My Requisitions" name="my_requisitions" domain="[('user_id', '=', uid)]"/>
                <separator/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]" help="New Agreements"/>
                <filter string="Confirmed" name="confirmed" domain="[('state', 'in', ('in_progress', 'open'))]" help="In negotiation"/>
                <filter string="Done" name="done" domain="[('state', '=', 'done')]"/>
                <separator/>
                <filter invisible="1" string="Late Activities" name="activities_overdue"
                        domain="[('activity_ids.date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                        help="Show all records which has next action date is before today"/>
                <filter invisible="1" string="Today Activities" name="activities_today"
                        domain="[('activity_ids.date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter invisible="1" string="Future Activities" name="activities_upcoming_all"
                        domain="[('activity_ids.date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                <group expand="0" string="Group By">
                    <filter string="Purchase Representative" name="representative" domain="[]" context="{'group_by': 'user_id'}"/>
                    <filter string="Status" name="status" domain="[]" context="{'group_by': 'state'}"/>
                    <filter string="Requisition Date" name="date_requisition" domain="[]" context="{'group_by': 'date_requisition'}"/>
                </group>
                
            </search>
        </field>
      </record>
      
    <record id="action_purchase_demand_view" model="ir.actions.act_window">
            <field name="name">Purchase Requistion</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">purchase.demand</field>
            <field name="view_mode">tree,kanban,form,activity</field>
            <field name="search_view_id" ref="purchase_demand_search_view"/>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new requisition!
                </p><p>
                    Once the requistion is confirmed.<br/> You will be able to create picking or purchase order.
                </p>
            </field>
    </record>
 
    <menuitem action="action_purchase_demand_view" id="menu_purchase_demand_action" parent="purchase.menu_procurement_management" sequence="1"/>


  </data>
</odoo>