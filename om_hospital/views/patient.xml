<odoo>
    
<!-- add one field in sale form -->
    <record id="view_sale_order_form_inhrt" model="ir.ui.view">
		<field name="name">sale.order.inherit</field>
		<field name="model">sale.order</field>
		<field name="inherit_id" ref="sale.view_order_form" />
		<field name="arch" type="xml">
			<xpath expr="//field[@name='partner_id']" position="after">
				<field name="Patient_name"/>
			</xpath>
		</field>
	</record>

    <record id="patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree string="Patients">
                <field name="name_seq" readonly="1"/>
                <field name="name"/>
                <field name="patient_age"/>
                <field name="gender"/>
                <field name="note"/>
            </tree>
        </field>
    </record>

    <record id="patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form string="Patients">
                <sheet>
<!--                sequence number for every form -->
                    <div class="oe_title">
                        <h1>
                            <field name="name_seq" readonly="1"/>
                        </h1>
                    </div>
<!--                end sequence -->
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="test"/>
                            <field name="gender"/>
                        </group>
                        <group>
                            <field name="patient_age"/>
                            <field name="age_group"/>
                        </group>
                        </group>
                    <group>
                        <field name="note"/>
                        <field name="image"/>
                    </group>
                </sheet>
<!--            chatter box where we can log note schedule activity etc -->
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids" options="{'post_refresh': 'recipients'}"/>
                </div>
<!--            end chatter -->
            </form>
        </field>
    </record>
        
    <record id="patient_search" model="ir.ui.view">
        <field name="name">hospital.patient.search</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <search string="Patints">
                <field name="name_seq"/>
                <field name="name"/>
                <field name="patient_age"/>
                <separator/>
                <filter string="Male" name="male" domain="[('gender', '=', 'male')]"/>
                <filter string="Female" name="female" domain="[('gender', '=', 'female')]"/>
                <filter string="Others" name="other" domain="[('gender', '=', 'other')]"/>
                <group expand ="1" string="Group By">
                    <filter string="Gender" name="gender" context="{'group_by':'gender'}"/>
                    <filter string="Patient Name" name="patient_name" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>  
   
    <!-- action of the patient menu -->
    <record id="patient_action" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="context">{'search_default_male': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                create your first patient!
            </p>
        </field>
    </record>

    <menuitem id="hospital_root"
              name="Usama Shakeel"
              web_icon="om_hospital,static/description/usama.jpg"
              sequence="10"/>
   

    <menuitem id="hospital_patient_root"
              name="Patients"
              parent="hospital_root"
              action="patient_action"
              sequence="10"/>

<!--     <menuitem id="test_menu"
              name="Menu"
              action="hr.open_view_employee_list_my"
              sequence="10"/> -->

</odoo>