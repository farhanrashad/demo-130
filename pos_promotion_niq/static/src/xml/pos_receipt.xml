<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-extend="OrderReceipt1">
        <t t-jquery=".pos-total" t-operation="before">
            <span class="pos-receipt-saving">Saving: <t t-esc='widget.format_currency(discount_total)'/></span>
        </t>
        <t t-jquery=".pos-line" t-operation="after">
            <div class="pos-discount">
                <div style="width:60%">Discount</div>
                <div style="width:20%"><t t-esc="widget.format_currency_no_symbol(line.promo_discount / line.qty)"/></div>
                <div style="width:20%"><t t-esc="widget.format_currency_no_symbol(line.promo_discount)"/></div>
            </div>
        </t>
    </t>

    <t t-extend="OrderReceipt">
        <t t-jquery=".pos-total" t-operation="before">
            <span class="pos-receipt-saving">Saving: <t t-esc='widget.format_currency(receipt.total_discount)'/></span>
        </t>
        <t t-jquery=".pos-line" t-operation="after">
            <div class="pos-discount">
                <div style="width:60%">Discount</div>
                <div style="width:20%"><t t-esc="widget.format_currency_no_symbol(line.promo_discount)"/></div>
                <div style="width:20%"><t t-esc="widget.format_currency_no_symbol(line.promo_discount * line.quantity)"/></div>
            </div>

            <!-- <div class='orderlines'>
                <t t-foreach='receipt.orderlines' t-as='line'>
                    <t t-set='simple' t-value='(line.discount === 0 and line.unit_name === "Units" and line.quantity === 1 and !discount_to_show and !(line.display_discount_policy == "without_discount" &amp;&amp; line.price != line.price_lst))' />
                    <t t-if='simple'>
                        <div><t t-esc='line.product_name_wrapped[0]' /><span t-esc='widget.format_currency_no_symbol(line.price_display)' class="price_display pos-receipt-right-align"/></div><t t-call="OrderReceiptWrappedProductNameLines"/></t>
                    <t t-if='!simple'>
                        <div t-esc='line.product_name_wrapped[0]'/><t t-call="OrderReceiptWrappedProductNameLines"/><t t-if="line.display_discount_policy == 'without_discount' &amp;&amp; line.price != line.price_lst"><div class="pos-receipt-left-padding"><t t-esc="widget.format_currency_no_symbol(line.price_lst)" />-><t t-esc="widget.format_currency_no_symbol(line.price)" /></div>
                        </t>
                        <t t-elif='line.discount !== 0'>
                            <div class="pos-receipt-left-padding">
                                <t t-if="pos.config.iface_tax_included === 'total'">
                                    <t t-esc="widget.format_currency_no_symbol(line.price_with_tax_before_discount)"/>
                                </t>
                                <t t-else="">
                                    <t t-esc="widget.format_currency_no_symbol(line.price)"/>
                                </t>
                            </div>
                        </t>
                        <t t-if='line.discount !== 0'>
                            <div class="pos-receipt-left-padding">
                                Discount: <t t-esc='line.discount' />%
                            </div>
                        </t>
                        <t t-if='line.get_promo_disc_percentage() !== 0'>
                            <div class="pos-receipt-left-padding">
                                Discount: <t t-esc='line.get_promo_disc_percentage()' />%
                            </div>
                        </t>
                        <div class="pos-receipt-left-padding">
                            <t t-esc="Math.round(line.quantity * Math.pow(10, pos.dp['Product Unit of Measure'])) / Math.pow(10, pos.dp['Product Unit of Measure'])"/>
                            <t t-if='line.unit_name !== "Units"' t-esc='line.unit_name'/>
                            x
                            <t t-esc='widget.format_currency_no_symbol(line.price_display_one)' />
                            <span class="price_display pos-receipt-right-align">
                                <t t-esc='widget.format_currency_no_symbol(line.price_display)'/>
                            </span>
                        </div>
                    </t>
                </t>
            </div> -->
            <!-- <table class='receipt-orderlines'>
                <colgroup>
                    <col width='50%' />
                    <col width='25%' />
                    <col width='25%' />
                </colgroup>
                <tr t-foreach="orderlines" t-as="orderline">
                    <td>
                        <t t-esc="orderline.get_product().display_name"/>
                         <t t-if="orderline.get_discount() > 0">
                            <div class="pos-disc-font">
                                With a <t t-esc="orderline.get_discount()"/>% discount
                            </div>
                        </t>
                        <t t-if="orderline.get_promo_disc_percentage() > 0">
                            <div class="pos-disc-font">
                                With a <t t-esc="orderline.get_promo_disc_percentage()"/>% promotion discount
                            </div>
                        </t>
                        <t t-if="orderline.get_promo_disc_amount() > 0">
                            <div class="pos-disc-font">
                                With discount <t t-esc="widget.format_currency(orderline.get_promo_disc_amount())"/>
                            </div>
                        </t>
                        <t t-if="orderline.get_promo_fixed_price() > 0">
                            <div class="pos-disc-font">
                                With fixed price <t t-esc="widget.format_currency(orderline.get_promo_fixed_price())"/>/<t t-esc="orderline.get_unit().name" />
                            </div>
                        </t>
                        <t t-if="orderline.get_promo_get_free() > 0">
                            <div class="pos-disc-font">
                                Get free with original price <t t-esc="widget.format_currency(orderline.get_origin_price())"/>/<t t-esc="orderline.get_unit().name" />
                            </div>
                        </t>
                    </td>
                    <td class="pos-right-align">
                        <t t-esc="orderline.get_quantity_str_with_unit()"/>
                    </td>
                    <td class="pos-right-align">
                        <t t-esc="widget.format_currency(orderline.get_display_price())"/>
                    </td>
                </tr>
            </table> -->
        </t>


    </t>

</templates>
